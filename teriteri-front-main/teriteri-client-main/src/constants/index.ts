export const TOKEN_KEY = 'teri_token'

export enum MsgType {
  REPLY = 0,
  AT = 1,
  LOVE = 2,
  SYSTEM = 3,
  WHISPER = 4,
  DYNAMIC = 5,
}

export enum WsCode {
  CONNECT = 100,
}

export enum UploadType {
  VIDEO = 'video',
  TEXT = 'text',
}
